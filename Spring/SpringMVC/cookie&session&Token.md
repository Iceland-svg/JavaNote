cookie一般存在客户端，一般存的是session id,session一般存在服务端，用来和客户端发过来的session id校验，验证用户登录状态,token是一个加密的令牌，登录成功后服务器生成一个令牌给用户保存，保存了用户状态信息，每次用户请求都会带着令牌，而服务器就只需要用密钥验签，这样服务器不用存储任何session信息，无论请求那台机器，服务器都认识--高可用，又因为无状态的token无法被吊销，不处理他会永远有效，只能等待过期，所以真实项目里会把token存在名单里
