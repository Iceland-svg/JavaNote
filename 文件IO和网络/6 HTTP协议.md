DNS
应用层协议
域名解析系统
现代DNS主要通过服务器来实现
DNS解决海量访问请求的方案?
1 相邻的几次访问，域名和ip的映射关系不会变
2 分布式系统
DNS服务器本来就有很多台（专门的组织手里），每个国家还有多台镜像服务器（同步数据）
http
除自定义应用层协议之外，也可以基于HTTP协议（现成的协议）来开发
一问一答式

报文格式

请求
1首行（方法URL版本号）
2请求头
3空行
4正文（body）
响应
1首行
2响应头
3空行
4正文


URL（唯一资源定位符）
协议名：//服务器ip（域名）：端口号/带层次的路径?key=value&key=value#片段标识符

ip，端口，路径，查询字符串
URL需要 URL encode（转义）防止value部分和特殊符号冲突
http方法

### HTTPS 

HTTPS是在HTTP的基础上引入一层SSL加密（加密层）