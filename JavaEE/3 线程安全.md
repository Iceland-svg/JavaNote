## 1 什么叫线程安全

某段代码在单线程的情况下正常运行，但是在多线程的情况下出现bug

代码实例

对于count++操作，其实是三个指令

load：把内存中的数据放到加载到cpu里
add：在cpu寄存器里对数据进行计算
save：将寄存器中的数据存回内存

画时间轴

由于count++不符合原子性，并且线程的调度是随机的

## 2 出现线程安全的原因

1 线程的调度是随机的（抢占式）执行

2 多个线程对同一个变量进行修改

如果把 count改为局部变量会怎么样?

贴图

lambda表达式变量捕获的变量必须是final
而作为成员变量时，则是以内部类访问外部类的形式
不可变对象天然线程安全

## 3 锁是什么?

锁的作用就是把若干个操作打包成一个，让其符合==原子性==

如果锁被占用，必须等待第一个锁解锁，这个时候第二个对象就处于阻塞状态，这时就产生了锁竞争，锁冲突

代码实例，运行结果实例

时间线解释为什么运行结果对了

实际上是，对count加锁了，他的三条指令必须执行完，才能执行另一个线程里的count，这是其实就是从穿插执行变为串行执行、
12.16
1启动线程- start（）

一个线程对象只能start一次，start之后线程处于就绪/阻塞状态，对于就绪/阻塞状态的线程不能start

2 线程是怎么中断的?
（1）运用变量标志位

运用实例

（2）运用线程内置标志位 isInterrupted（）

运用实例（在lambda表达式内部用t时，此时t还未被创建，需要用Thread.currentThread获取该线程的引用，在那个线程里调用获取的就是那个线程的引用）

为什么支持提前唤醒正在sleep的线程?
当sleep被提前终止时会抛出iterruptException异常
同时把isIterrupted（）返回值改为false

他的主要目的是为了给程序员更多的操作空间，因为sleep还没睡够就被唤醒了，是否会存在一些未完成工作
该异常的三种处理方式
1 立刻终止
2忽略
3 添加善后逻辑，稍后结束

3我们为什么需要线程等待?

操作系统在调度线程的时候是随机的（抢占式执行），作为程序而言通常是不喜欢随机的，所以这个时候我们需要通过join（）方法来让要后结束的线程阻塞，等待要先结束的线程，执行完

代码示例
让主线程等待
让t线程等待
谁调join（），谁先结束，另一个必须等待
join（）有一个重载版本，可以指定等待的最大时间

4线程状态
new

Thread对象创建了，但是还没start

terminated
线程执行完了
Thread对象还在

这里注意操作系统中线程的生命周期和Thread对象的不一致
Runnable
只要不触发阻塞都是Runnable状态

BLOCKE加锁产生的阻塞
WATING无时间的阻塞
TIME_WATING有时间的阻塞，join有参数版本或者sleep
案例演示








线程安全

某段代码在单线程的情况下正常运行，但是在多线程的情况下出现bug

代码实例
对于count++操作，其实是三个指令
load：把内存中的数据放到加载到cpu里
add：在cpu寄存器里对数据进行计算
save：将寄存器中的数据存回内存
画时间轴
由于count++不符合原子性，并且线程的调度是随机的

出现线程安全的原因
1 线程的调度是随机的（抢占式）执行
2 多个线程对同一个变量进行修改
如果把 count改为局部变量会怎么样?
如果把synchronized放到外面会如何?
代码实例
贴图
lambda表达式变量捕获的变量必须是final
而作为成员变量时，则是以内部类访问外部类的形式
不可变对象天然线程安全

锁是什么?

把若干个操作打包成一个，让其符合原子性
（1）互斥
如果锁被占用，必须等待第一个锁解锁，这个时候第二个对象就处于阻塞状态，这时就产生了锁竞争，锁冲突

代码实例，运行结果实例

时间线解释为什么运行结果对了
实际上是，对count加锁了，他的三条指令必须执行完，才能执行另一个线程里的count，从穿插执行变为串行执行


如果把synchronized放到外面，那么就是给整个线程加锁，必须让整个线程执行完，另一个线程才能从阻塞状态开始执行
为啥花的时间更少了?
因为加锁产生的锁冲突也需要处理时间

synchronized如何使用?

代码实例

我的理解，这里应该是用locker把执行流程打包（或者说synchronized后大括号里的代码块），解锁之后别的线程才能拿到这个锁，
没解锁别的线程就要一直等待阻塞

1基础用法synchronized（锁对象）｛｝

2给某个方法加上synchronized修饰
代码实例
锁对象是啥对象不重要，重要的是两个线程是否针对同一个对象加锁

两种写法单独领出来

3 synchronized修饰静态方法
static修饰的方法是类方法不存在实例，没有this

代码示例

synchronized的设计理念是什么？

（2）可重入
第一种死锁
如果加锁的时候这个锁已经是被加锁状态，就会触发锁竞争，这个锁就会一直阻塞等待锁被释放
代码实例
从逻辑上看
走到第二个加锁位置，触发阻塞，要解除阻塞，需要释放第一个锁，但是要释放第一个锁，就得把第二个锁加上
死锁

不明显的死锁实例

为此Java中引入可重入机制解决上述死锁问题
所以满足一个线程，同一把锁，连续加锁多次，不会触发死锁，这个锁可以成为可重入锁

第二种死锁
两个线程两把锁，每个线程先获取到一把锁，
再尝试获取到对方的锁
代码实例

第三种死锁N个线程M把锁
循环等待
如何避免死锁问题?

首先要理解死锁的四个必要条件

1锁本身是互斥的
2锁不可被抢占，线程1拿到锁，线程2想要这个锁必须要阻塞等待
3请求和保持 线程在拿到第一把锁的情况下，不释放第一把所，而去请求第二把锁
（车钥匙锁家里了，家里钥匙锁车里了）
4循环等待，等待锁的释放构成循环

解决方案
让锁先释放再去拿另一个锁
打破循环等待，约定加多个锁的时候，必须按照一定的顺序来加锁

内存可见性引起的线程安全

代码引入

编译器优化，编译器会分析代码的意图，自动优化程序，
这里编译器发现，循环过程中，反复去从内存中load flag变量，开销很大，所以就干脆从读好的寄存器中获取flag值，以此来大大提高循环的效率，又因为是让用户输入flag，编译器不知道这段修改flag代码，能不能执行

volatile
关键字，提醒编译器某个变量是易变的，不要优化这个变量

代码实例

jvm在运行时，会在读写volatile修饰的变量的指令前后添加内存屏障相关的指令
Java内存模型
main memory 就是内存
work memory 其实是寄存器，和缓存

为什么加了sleep就不再优化?
此时性能瓶颈就不再是load的过程，优化它也没啥用

代码实例

wait/notify
根本上还是由于线程的随机调度引起
由于随机调度问题，所以需要用wait和/notify来协调线程的执行顺序
线程饿死，线程一释放锁之后自己也会参与到锁竞争，但是其他线程由于唤醒需要时间，很可能线程一会捷足先登

使用案例
wait（）方法内部做的第一件事就是释放锁

运用实例

wait做了什么?

阻塞当前代码，同时释放该线程的锁，满足一定条件时被唤醒，尝试重新获取这个锁

wait阻塞的两个阶段

1 WATING状态的阻塞，通过wait等待其他线程的通知

2 BLOCKED的阻塞，收到通知之后就会尝试重新获取锁，很可能遇到锁竞争

notify唤醒，notifyAll唤醒全部线程



面试题sleep和wait的区别

1 设计理念不一样
wait设计出来就是为了提前唤醒的，设置等待时间上线，只是后手计划
sleep设计出来是为了到特定时间唤醒，到时间唤醒的，虽然也可以通过interrupt（）方法提前唤醒，但是这样的唤醒是会产生异常的（程序不符合预期）

2 wait需要搭配锁来使用，并且阻塞的同时会释放锁
sleep放到synchronized内部时，则是不会释放锁，一直带着锁阻塞

多线程运用实例







Java里的设计模式是什么？
可以理解为代码风格指南，规范模式

什么是单例模式?
一个进程只有唯一一个实例
饿汉模式

代码

设计原理：直接在类内部把唯一实例创建出来，并且设为静态，此时在类加载的时候，实例就创建了，并且构造方法置为private，不让在外部new

懒汉模式

代码

设计原理：也是先给出静态唯一实例，但是想让这个实例为null，在调get时候再new
，也让构造为private

为什么要把if也包在synchronized里面?

为什么要在synchronized外面在判定一次 ?

代码

第二次调用时避免重复加锁
内部if的作用是什么?
单线程的时候判定第一次为空，第二次也一定为空，然后创建
多线程时，假如有两个线程，同时调getInstance，开始时两个都会进入第一个if里面，接着谁先拿到锁谁就先创建实例，然后释放锁，另一个没创建实例的拿到锁，此时if判定instance不为空，避免重复创建实例
所以这里外层检查的目的就是提高性能，避免每次重复加锁，内层检查是为了避免破坏单例


指令重排序引起的线程安全问题

指令重排序是编译器的一种优化策略，为的是调整指令的顺序来优化程序执行更快

但是，在这种优化策略下的多线程，执行new操作时，（主要有三个步骤，申请空间，初始化，把内存地址保存到引用变量中）
优化之后，先申请空间，在存地址到引用变量，如果在申请空间和存地址到引用变量之后调度到另一个线程，这时如果这个线程调get，拿到实例，使用时，会发现该实例并没有初始化
这里最关键的是，读操作是不需要锁的，假如另一个线程检查instance!=null后，会直接返回了，返回的instance由于指令重排序初始化可能不完整，调get操作得到的就是不完整的instance，锁不能阻止其他变量读取共享变量!!!这个变量是共享的虽然，锁让变量只能让第一个线程来new
解决方案
在变量前加上volatile，让这个变量在读写操作时不要触发优化





阻塞队列

1线程安全
2带有阻塞功能
-如果队列为空，尝试出队列，就会触发阻塞知道队列不为空
-如果队列为满，尝试入队列，就会触发阻塞，知道队列不为满

生产者消费模型（是分布式系统的一种重要设计模式）

1减少资源锁竞争，提高效率
2跟好的做到模块之间的解耦
3削峰填谷

分布式系统
服务器开发通常是开发一组服务器程序

阻塞队列在分布式系统中的作用是什么?

1削峰填谷，请求多的时候会阻塞，防止超过资源提供上限，导致服务器挂了
2解耦合

缺点：系统更加复杂，引入的队列层数太多，会增加网络通信的开销


标准库中的BockingQueue

验证阻塞效果代码

生产者消费者模式运用

自实现阻塞队列

代码

为什么把if改为while?
这里的while其实是二次确认，如果存在其他的代码，让if确认进入wait阻塞后，任然是满的，程序接着往下执行就违背了初衷
所以官方建议利用while，在被唤醒之后，由于循环机制，再次确认是否满了

线程池

频繁创建销毁开销很大，提前把线程创建好放到池子里面
目的是提高效率

核心线程：
一创建就申请的线程
临时线程：
核心线程处理不过来了就申请一些其他线程来帮忙，帮忙的就是临时线程
不忙的时候就释放临时线程，但是核心线程永远不会销毁，除非销毁整个线程池

线程池类的参数
1 int corepoolSize 
核心线程数

2 int maximumPoolSize
最大线程数

3 long KeepAliveTime
线程的存活时间

4 TimeUnit unit
线程允许休息的最大时间

5 BlockingQueue<Runnable> workQueye
任务队列，里面放的都是线程要执行的任务，每个任务都是Runnable来描述的，使用线程池的时候，先会创建线程，然后需要调用者放一些任务，线程就会从任务队列里取出任务来执行

6 ThreadFactory threadFactory
线程工厂类
工厂模式的设计初衷是为了弥补构造方法的缺陷，因为在构造方法中，为满足不同构造需求可能会出现需要重载的情况，重载要求我们方法名字相同，参数列表不同，但是实际上就是可能会出现参数列表也相同的构造方法，所以我们不用构造方法，让这个类不包含构造方法，单独写一个工厂类，在内部写静态方法，这样可以实现不同名字不同参数列表的构造

代码案例

7 RejectedExecutionHandler handler
拒绝策略
如果任务队列满了，我们又再次添加新任务，这时候按道理是会阻塞的，但是在实际情景中，对于某个程序，如果需要高时效性，这里绝对不能阻塞

四种拒绝策略
1 AbortPolicy
直接终止
2 CallerRunsPolicy
让调用者来执行任务，调用者也是一个线程
3 DiscardOldestPolicy
丢弃掉最老的任务
4 DiscardPolicy
丢弃掉最新的任务

Executor （简化版本）
本质上也是工厂类，创建不同风格的线程池

运用实例代码

利用submit给任务

自实现线程池

代码

使用该线程池

代码

定时器

阻塞队列由于非常重要，一般会被单独封装成一个，一组服务器
定时器也是类似，也会别单独封装成一个或者一组服务器
等到一定时间去执行
Timer

运用实例代码

自实现定时器

代码

最大等待时间
进行周期性工作