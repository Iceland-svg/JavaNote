（1）加上@ControllerAdvice  注解
——————此注解允许在@RestController把返回结果写入http响应结果之前对响应结果修改

（2）实现ResponseBodyAdvice接口，重写beforeBodyWrite方法

（3）**对 String 特殊处理**：由于 Spring MVC 处理 `String` 返回值时，会使用 `StringHttpMessageConverter` 直接输出字符串，如果直接返回 `Result` 对象，会报类型转换错误。因此手动将包装后的 `Result` 序列化为 JSON 字符串

```java
@ControllerAdvice  
@Slf4j  
public class ResponseAdvice implements ResponseBodyAdvice {  
    @Autowired  
    private ObjectMapper objectMapper;  
    @Override  
    public boolean supports(MethodParameter returnType, Class converterType) {  
        return false;  
    }  
  //这个注解自动包上try,catch
    @SneakyThrows  
    @Nullable    @Override    public Object beforeBodyWrite(@Nullable Object body, MethodParameter returnType, MediaType selectedContentType, Class selectedConverterType, ServerHttpRequest request, ServerHttpResponse response) {  
        log.info("执行beforeBodyWrite方法");  
        if(body instanceof Result<?>){  
            return body;  
        }  
        if(body instanceof String){  
            return objectMapper.writeValueAsString(Result.success(body));  
        }  
        return Result.success(body);  
    }  
}
```