
模拟多机器部署同一个服务

![](assets/6%20负载均衡/file-20260614215344360.png)

这个时候访问order服务发现全部请求只丢给了一个实例，所以需要负载均衡来实现分流减压

## 客户端负载均衡 - SpringCloudLoadBalancer用法

添加注解 @LoadBalanced
修改远程调用代码，把ip和端口号改成应用名

```java
public OrderInfo selcetOrderInfoById(Integer orderId){  
    OrderInfo orderInfo = orderMapper.SelectOrderById(orderId);  
    String url = "http://product-service/product/"+ orderInfo.getProductId();  
    log.info("远程调用url: {}",url);  
    ProductInfo productInfo = restTemplate.getForObject(url, ProductInfo.class);  
    orderInfo.setProductInfo(productInfo);  
    return orderInfo;  
}
```


**负载均衡策略**：轮询，随机选择，自定义

**自定义策略**

![700](assets/6%20负载均衡/file-20260811162830684.png)

**搭配@LoadBalancerClient实现自定义负载均衡策略**

![](assets/6%20负载均衡/file-20260811163844630.png)
